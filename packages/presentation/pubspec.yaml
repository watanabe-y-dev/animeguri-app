name: presentation
description: "Presentation layer for animeguri."
version: 0.0.1

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Local packages
  foundation:
  model:

dev_dependencies:
  build_runner: ^2.4.9

flutter:
