name: app
publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Local packages
  data:
    path: ../data
  domain:
    path: ../domain
  foundation:
    path: ../foundation
  model:
    path: ../model
  presentation:
    path: ../presentation

  firebase_core: ^3.5.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  very_good_analysis: ^8.0.0

flutter:
  uses-material-design: true
