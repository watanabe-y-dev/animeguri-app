name: data
description: 'Data layer for animeguri.'

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  # Local packages
  domain:
    path: ../domain
  foundation:
    path : ../foundation
  model:
    path : ../model
  presentation:
    path : ../presentation

dev_dependencies:
  build_runner: ^2.4.9
  very_good_analysis: ^7.0.0
