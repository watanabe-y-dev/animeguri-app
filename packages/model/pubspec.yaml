name: model
description: 'Model for animeguri.'
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  # No dependencies

dev_dependencies:
  build_runner: ^2.4.9
  very_good_analysis: ^5.1.0
