name: domain
description: 'Domain layer for animeguri.'
version: 0.0.1

publish_to: 'none'

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  # Local packages
  foundation:
  model:
  presentation:

dev_dependencies:
  build_runner: ^2.4.9
