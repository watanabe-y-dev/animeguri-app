---
description:
globs:
alwaysApply: true
---
## `dart run build_runner` について

`dart run build_runner` を使用する代わりに `melos gen` コマンドを使用してください。

## `flutter pub get` について

`flutter pub get` を使用する代わりに `melos get` コマンドを使用してください。
